---
title: "二叉树的直径"
date: 2020-03-10T22:35:24+08:00
categories:
  - acwing力扣打卡第二期
tags:
  - 字节跳动
  - 树
---

# 题目

![](/images/%E5%89%91%E6%8C%87offer/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E7%9B%B4%E5%BE%84.png)

# 综述

-   对每个节点来说：树的直径为左子树的深度+右子树的深度
-   **在求树的深度的过程中得到二叉树的直径**
-   之后做二叉树中的最大路径和

# Code

```c++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Solution {
public:
    int res=0;
    int diameterOfBinaryTree(TreeNode* root) {
        depth(root);
        return res;
    }
    int depth(TreeNode* root){
        if(!root)return 0;
        int left_depth=depth(root->left);
        int right_depth=depth(root->right);
        res=max(res,left_depth+right_depth);
        return max(left_depth,right_depth)+1;
    }
};
```

# 复杂度分析
