---
title: "删除排序链表中的重复元素"
date: 2021-02-07T00:17:03+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 链表
---

# 题目

![image-20210207175505214](https://cjx132.github.io/picture-bed/img/image-20210207175505214.png)

# 综述

-   和删除排序数组中的重复元素思路一样

# Code

```c++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* deleteDuplicates(ListNode* head) {
        if(!head||head->next==nullptr)return head;

        ListNode* first=head->next,*second=head;

        while(first)
        {
            if(second->val<first->val)
            {
                second->next=first;
                second=second->next;
            }
            first=first->next;
        }
        second->next=nullptr;//重要

        return head;
    }
};
```

# 复杂度分析

-   时间复杂度：O(n)
    
-   空间复杂度：O(1)
