---
title: "最长公共前缀"
date: 2021-02-21T00:08:54+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 字符串
---

# 题目

![image-20210221170803211](https://cjx132.github.io/picture-bed/img/image-20210221170803211.png)

# 综述

-   纵向扫描
-   纵向扫描时，从前往后遍历所有字符串的每一列，比较相同列上的字符是否相同，如果相同则继续对下一列进行比较，如果不相同则当前列不再属于公共前缀，当前列之前的部分为最长公共前缀。

# Code

```c++
class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
        int n=strs.size();
        if(!n)return "";
        int m=strs[0].size();

        for(int i=0;i<m;i++)
            for(int j=1;j<n;j++)
                if(i==strs[j].size()||strs[j][i]!=strs[0][i])
                    return strs[0].substr(0,i);
        return strs[0];
    }
};
```

# 复杂度分析
