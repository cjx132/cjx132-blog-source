---
title: "下一个更大元素2"
date: 2021-02-19T13:13:47+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 单调栈
---

# 题目

![image-20210219021853762](https://cjx132.github.io/picture-bed/img/image-20210219021853762.png)

# 综述

-   使用取模模拟环状
-   单调栈模板题：求右边第一个xxx：倒序，左边第一个xxx：正序

# Code

```c++
class Solution {
public:
    vector<int> nextGreaterElements(vector<int>& nums) {
        stack<int>stk;
        int n=nums.size();
        vector<int>ans(n);
        for(int i=2*n-1;i>=0;i--)
        {
            while(!stk.empty()&&nums[i%n]>=nums[stk.top()])
                stk.pop();
            if(stk.size())ans[i%n]=nums[stk.top()];
            else ans[i%n]=-1;
            stk.push(i%n);
        }
        return ans;
    }
};
```

# 复杂度分析
