---
title: "岛屿数量"
date: 2020-08-28T19:19:55+08:00
categories:
  - acwing力扣打卡第二期
tags:
  - 字节跳动
  - 搜索
---

# 题目

![](/images/Top100/%E5%B2%9B%E5%B1%BF%E6%95%B0%E9%87%8F.png)

# 综述

# Code

```c++
class Solution {
public:
    int dx[4]={-1,0,1,0},dy[4]={0,1,0,-1};
    int numIslands(vector<vector<char>>& grid) {
        int res=0;
        for(int i=0;i<grid.size();i++)
            for(int j=0;j<grid[0].size();j++)
                if(grid[i][j]=='1')
                {
                    res++;
                    dfs(grid,i,j);
                }
        return res;
    }

    void dfs(vector<vector<char>>& grid,int x,int y)
    {
        for(int i=0;i<4;i++)
        {
            int tx=x+dx[i],ty=y+dy[i];
            if(tx>=0&&tx<grid.size()&&ty>=0&&ty<grid[0].size()&&grid[tx][ty]=='1')
            {
                grid[tx][ty]='0';
                dfs(grid,tx,ty);
            }
        }
    }
};
```

# 复杂度分析
