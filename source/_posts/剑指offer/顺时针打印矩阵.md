---
title: "顺时针打印矩阵"
date: 2020-03-03T17:10:36+08:00
categories:
  - 剑指offer
tags:
  - 模拟
---

# 题目

![](/images/%E5%89%91%E6%8C%87offer/%E9%A1%BA%E6%97%B6%E9%92%88%E6%89%93%E5%8D%B0%E7%9F%A9%E9%98%B5.png)

# 综述

-   定义四个边界（边界的值随着遍历逐渐变化）
-   四个方向依次遍历（1.遍历结束修改边界值；2.边界值修改之后判断是否满足条件）

# Code

```plain
class Solution {
public:
    vector<int> spiralOrder(vector<vector<int>>& matrix) {
        vector<int>ans;
        if(matrix.size()==0||matrix[0].size()==0)return ans;
        int top=0;
        int bottom=matrix.size()-1;
        int left=0;
        int right=matrix[0].size()-1;
        while(1){
            //从左到右
            for(int i=left;i<=right;i++)ans.push_back(matrix[top][i]);
            top++;
            if(top>bottom)break;

            //从上往下
            for(int i=top;i<=bottom;i++)ans.push_back(matrix[i][right]);
            right--;
            if(right<left)break;

            //从右往左
            for(int i=right;i>=left;i--)ans.push_back(matrix[bottom][i]);
            bottom--;
            if(bottom<top)break;

            //从下往上
            for(int i=bottom;i>=top;i--)ans.push_back(matrix[i][left]);
            left++;
            if(left>right)break;
        }
        return ans;
    }
};
```

# 复杂度分析
