---
title: "四数之和"
date: 2021-08-14T13:13:48+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 双指针
---

# 题目

![image-20210815114053465](https://cjx132.github.io/picture-bed/image-20210815114053465.png)

# 综述

-   相比三数之和多加了一层遍历

# Code

```c++
class Solution {
public:
    vector<vector<int>> fourSum(vector<int>& nums, int target) {
        vector<vector<int>>ans;
        int n=nums.size();
        if(n<4)return ans;
        sort(nums.begin(),nums.end());

        for(int a=0;a<n-3;a++)
        {
            if(a>0&&nums[a]==nums[a-1])continue;
            for(int b=a+1;b<n-2;b++)
            {
                if(b>a+1&&nums[b]==nums[b-1])continue;
                int c=b+1,d=n-1;
                while(c<d)
                {
                    long sum=(long)nums[a]+(long)nums[b]+(long)nums[c]+(long)nums[d];
                    int left=nums[c],right=nums[d];
                    if(sum>target)
                    {
                        while(c<d&&right==nums[d])d--;
                    }
                    else if(sum<target)
                    {
                        while(c<d&&left==nums[c])c++;
                    }
                    else
                    {
                        ans.push_back({nums[a],nums[b],nums[c],nums[d]});
                        while(c<d&&right==nums[d])d--;
                        while(c<d&&left==nums[c])c++;
                    }
                }
            }
        }
        return ans;
    }
};
```

# 复杂度分析

-   时间复杂度：O(n^3)
-   空间复杂度：O(n^3)
