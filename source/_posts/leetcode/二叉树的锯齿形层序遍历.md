---
title: "二叉树的锯齿形层序遍历"
date: 2021-02-08T13:13:46+08:00
categories:
  - 剑指offer
tags:
  - 字节跳动
  - 树
---

# 题目

![image-20210208223621049](https://cjx132.github.io/picture-bed/img/image-20210208223621049.png)

# 综述

-   翻来翻去烦不烦？为什一定要插入前面或者后面啊，每一层的大小都知道了，直接在对应索引处赋值不行吗

# Code

```c++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Solution {
public:
    vector<vector<int>> zigzagLevelOrder(TreeNode* root) {
        queue<TreeNode*>q;
        vector<vector<int>>ans;
        if(root)q.push(root);
        bool f=true;
        while(!q.empty())
        {
            int n=q.size();
            vector<int>tmp(n,0);
            for(int i=0;i<n;i++)
            {
                auto t=q.front();
                q.pop();
                tmp[f?i:n-i-1]=t->val;
                if(t->left)q.push(t->left);
                if(t->right)q.push(t->right);
            }
            ans.push_back(tmp);
            f=!f;
        }
        return ans;
    }
};
```

# 复杂度分析
