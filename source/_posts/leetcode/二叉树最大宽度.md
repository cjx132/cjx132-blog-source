---
title: "二叉树最大宽度"
date: 2021-02-22T13:13:46+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 树
---

# 题目

![image-20210222011158780](https://cjx132.github.io/picture-bed/img/image-20210222011158780.png)

# 综述

-   层次遍历：**树中的空节点不能入队**
-   1.  保存每个节点在完全二叉树中的节点编号
    2.  逐层遍历，每层扩展之前，求出上一层的最大宽度
    3.  存在溢出的情况，使用unsigned long long
-   优化版
    1.  val字段存储节点编号
    2.  每层扩展之前，把上一层第一个节点的编号从1开始，上一层后面节点的编号都减去同一个值

# Code

```c++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    int widthOfBinaryTree(TreeNode* root) {
        queue<pair<TreeNode*,unsigned long long>>q;
        q.push({root,1});
        unsigned long long res=0;
        while(!q.empty())
        {
            res=max(res,q.back().second-q.front().second+1);//求出上一层的最大宽度
            int n=q.size();
            for(int i=0;i<n;i++)
            {
                auto t=q.front();
                q.pop();
                if(t.first->left)
                    q.push({t.first->left,t.second*2});
                if(t.first->right)
                    q.push({t.first->right,t.second*2+1});
            }
        }
        return res;
    }
};
```
```c++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    int widthOfBinaryTree(TreeNode* root) {
        root->val=1;
        queue<TreeNode*>q;
        q.push(root);
        int res=0;
        while(!q.empty())
        {
            res=max(res,q.back()->val-q.front()->val+1);//求出上一层的最大宽度
            int tmp=q.front()->val;//上一层第一个节点的编号
            int n=q.size();
            for(int i=0;i<n;i++)
            {
                auto t=q.front();
                q.pop();
                int index=t->val-tmp+1;//上一层最左边节点编号从1开始
                if(t->left)
                {
                    t->left->val=index*2;
                    q.push(t->left);
                }
                if(t->right)
                {
                    t->right->val=index*2+1;
                    q.push(t->right);
                }
            }
        }
        return res;
    }
};
```

# 复杂度分析
