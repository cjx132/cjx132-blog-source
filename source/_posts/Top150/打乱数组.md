---
title: "打乱数组"
date: 2020-11-27T00:44:53+08:00
categories:
  - Top150
tags:
  - 洗牌算法
---

# 题目

![image-20201127004610161](https://cjx132.github.io/picture-bed/img/image-20201127004610161.png)

# 综述

洗牌算法模板题：缺点必须知道数组长度n。

[关于洗牌算法的讲解](https://zhuanlan.zhihu.com/p/149414584)

# Code

```c++
class Solution {
public:
    vector<int> res;
    vector<int> ans;
    Solution(vector<int>& nums) {
        res=nums;
        ans=nums;
    }
    
    /** Resets the array to its original configuration and return it. */
    vector<int> reset() {
        return res;
    }
    
    /** Returns a random shuffling of the array. */
    vector<int> shuffle() {
        for(int i=ans.size()-1;i>=0;i--){
            int index=rand()%(i+1);
            swap(ans[index],ans[i]);
        }
        return ans;
    }
};

/**
 * Your Solution object will be instantiated and called as such:
 * Solution* obj = new Solution(nums);
 * vector<int> param_1 = obj->reset();
 * vector<int> param_2 = obj->shuffle();
 */
```

# 复杂度分析

-   时间复杂度：O(n)
-   空间复杂度：O(1)
