---
title: "求区间最小数乘区间和的最大值"
date: 2021-02-19T20:17:04+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 单调栈
---

# 题目

![image-20210219204206305](https://cjx132.github.io/picture-bed/img/image-20210219204206305.png)

# 综述

枚举每个元素（设为x）作为区间最小值，在x左右两侧找到第一个比x小的元素，分别记录左右边界的下标为l,r，寻找边界时计算当前区间的和。那么以x为区间最小值的最大计算区间就是`[l+1,r-1]区间和*x`，枚举时更新最大值

和柱状图中最大的矩形思路一致

1.  哨兵，原始数组前后各一个-1
2.  使用单调栈找到左右两边第一个小于当前数的下标
3.  维护一个前缀和数组

# Code

```c++
#include<bits/stdc++.h>
using namespace std;
const int N=5e5+10;
int q[N],sum[N];
int n;
int main()
{
    scanf("%d",&n);
    for(int i=1;i<=n;i++)
    {
        scanf("%d",&q[i]);
        sum[i]=sum[i-1]+q[i];
    }
    q[0]=-1,q[n+1]=-1;
    stack<int>stk;
    int res=0;
    for(int i=0;i<=n+1;i++)
    {
        while(!stk.empty()&&q[i]<q[stk.top()])
        {
            int t=stk.top();
            stk.pop();
            while(!stk.empty()&&q[stk.top()]==q[t])
                stk.pop();
            if(stk.size())
                res=max(res,q[t]*(sum[i-1]-sum[stk.top()]));
        }
        stk.push(i);
    }
    printf("%d\n",res);
    return 0;
}
```

# 复杂度分析
