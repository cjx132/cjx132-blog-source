---
title: "删除排序链表中的重复元素2"
date: 2021-02-21T00:17:03+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 链表
---

# 题目

![image-20210221105607907](https://cjx132.github.io/picture-bed/img/image-20210221105607907.png)

# 综述

1.  **先拿出第一个元素（三数之和思路）**，如果这个元素和下一个元素不相等或者下一个元素为空，则拼接上
2.  否则，循环直到元素为空或者不相等
3.  **最后p指针的末尾加上一个空指针**

# Code

```c++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* deleteDuplicates(ListNode* head) {
        ListNode *dummy=new ListNode(-1);
        dummy->next=head;

        ListNode *p=dummy;
        ListNode *cur=dummy->next;
        
        while(cur)
        {
            auto t=cur->val;
            if(!cur->next||cur->next->val!=t)
            {
                p->next=cur;
                p=p->next;
                cur=cur->next;
            }
            else
                while(cur&&t==cur->val)
                    cur=cur->next;
            
        }
        p->next=nullptr;
        return dummy->next;
    }
};
```

# 复杂度分析

-   时间复杂度：O(n)
    
-   空间复杂度：O(1)
