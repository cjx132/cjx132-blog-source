---
title: "扁平化嵌套列表迭代器"
date: 2020-11-21T17:23:25+08:00
categories:
  - Top150
tags:
  - 双端队列
---

# 题目

![image-20201121172522256](https://cjx132.github.io/picture-bed/img/image-20201121172522256.png)

# 综述

-   看做一个n叉树
-   dfs解法：一次将结果全部保存
-   bfs解法：边计算边迭代（作为迭代器效率较高）

# Code

dfs解法

```c++
/**
 * // This is the interface that allows for creating nested lists.
 * // You should not implement it, or speculate about its implementation
 * class NestedInteger {
 *   public:
 *     // Return true if this NestedInteger holds a single integer, rather than a nested list.
 *     bool isInteger() const;
 *
 *     // Return the single integer that this NestedInteger holds, if it holds a single integer
 *     // The result is undefined if this NestedInteger holds a nested list
 *     int getInteger() const;
 *
 *     // Return the nested list that this NestedInteger holds, if it holds a nested list
 *     // The result is undefined if this NestedInteger holds a single integer
 *     const vector<NestedInteger> &getList() const;
 * };
 */

class NestedIterator {
public:
    queue<int>res;
    NestedIterator(vector<NestedInteger> &nestedList) {
        add(nestedList);
    }
    void add(vector<NestedInteger> &nestedList){
        for(auto item:nestedList){
            if(item.isInteger()){
                res.push(item.getInteger());
            }else{
                add(item.getList());
            }
        }
    }

    int next() {
        int top=res.front();
        res.pop();
        return top;
    }
    
    bool hasNext() {
        return !res.empty();
    }
};

/**
 * Your NestedIterator object will be instantiated and called as such:
 * NestedIterator i(nestedList);
 * while (i.hasNext()) cout << i.next();
 */
```

bfs解法

```c++
/**
 * // This is the interface that allows for creating nested lists.
 * // You should not implement it, or speculate about its implementation
 * class NestedInteger {
 *   public:
 *     // Return true if this NestedInteger holds a single integer, rather than a nested list.
 *     bool isInteger() const;
 *
 *     // Return the single integer that this NestedInteger holds, if it holds a single integer
 *     // The result is undefined if this NestedInteger holds a nested list
 *     int getInteger() const;
 *
 *     // Return the nested list that this NestedInteger holds, if it holds a nested list
 *     // The result is undefined if this NestedInteger holds a single integer
 *     const vector<NestedInteger> &getList() const;
 * };
 */

class NestedIterator {
public:
    deque<NestedInteger>res;
    NestedIterator(vector<NestedInteger> &nestedList) {
        for(auto item:nestedList){
            res.push_back(item);
        }
    }

    int next() {
        int top=res.front().getInteger();
        res.pop_front();
        return top;
    }
    
    bool hasNext() {
        while(!res.empty()){
            NestedInteger temp=res.front();
            if(temp.isInteger()){
                return true;
            }else{
                res.pop_front();
                vector<NestedInteger>vt=temp.getList();
                for(auto it=vt.rbegin();it!=vt.rend();it++){
                    res.push_front(*it);
                }
            }
        }
        return false;
    }
};

/**
 * Your NestedIterator object will be instantiated and called as such:
 * NestedIterator i(nestedList);
 * while (i.hasNext()) cout << i.next();
 */
```

# 复杂度分析
