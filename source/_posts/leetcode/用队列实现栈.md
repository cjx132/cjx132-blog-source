---
title: "用队列实现栈"
date: 2021-08-12T13:13:47+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 队列
---

# 题目

![image-20210812221157724](https://cjx132.github.io/picture-bed/img/image-20210812221157724.png)

# 综述

[参考](https://www.acwing.com/solution/content/322/)

# Code

```c++
class MyStack {
public:
    /** Initialize your data structure here. */
    queue<int>q;
    MyStack() {

    }
    
    /** Push element x onto stack. */
    void push(int x) {
        q.push(x);
    }
    
    /** Removes the element on top of the stack and returns that element. */
    int pop() {
        int n=q.size();
        while(n-- >1)q.push(q.front()),q.pop();
        int x=q.front();
        q.pop();
        return x;
    }
    
    /** Get the top element. */
    int top() {
        int n=q.size();
        while(n-- >1)q.push(q.front()),q.pop();
        int x=q.front();
        q.push(x),q.pop();
        return x;
    }
    
    /** Returns whether the stack is empty. */
    bool empty() {
        return q.empty();
    }
};

/**
 * Your MyStack object will be instantiated and called as such:
 * MyStack* obj = new MyStack();
 * obj->push(x);
 * int param_2 = obj->pop();
 * int param_3 = obj->top();
 * bool param_4 = obj->empty();
 */
```

# 复杂度分析
