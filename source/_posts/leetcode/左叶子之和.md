---
title: "最长递增子序列的个数"
date: 2021-02-18T00:08:54+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 树
---

# 题目

![image-20210218005357718](https://cjx132.github.io/picture-bed/img/image-20210218005357718.png)

# 综述

-   注意如何判断左叶子

# Code

```c++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Solution {
public:
    int sumOfLeftLeaves(TreeNode* root) {
        if(!root)return 0;
        int res=0;
        if(root->left&&!root->left->left&&!root->left->right)
            res+=root->left->val;
        return res+sumOfLeftLeaves(root->left)+sumOfLeftLeaves(root->right);
    }
};
```

# 复杂度分析
