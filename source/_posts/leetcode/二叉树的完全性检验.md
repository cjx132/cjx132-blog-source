---
title: "二叉树的完全性检验"
date: 2021-02-21T13:13:46+08:00
categories:
  - leetcode
tags:
  - 字节跳动
  - 树
---

# 题目

![image-20210221020503527](https://cjx132.github.io/picture-bed/img/image-20210221020503527.png)

# 综述

-   层次遍历
-   1.  第一次在层次遍历序列中发现空指针时就标记
    2.  层次遍历过程中，如果发现当前非空指针前面出现过空指针，此时就不是完全二叉树了，直接返回false。

# Code

```c++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    bool isCompleteTree(TreeNode* root) {
        queue<TreeNode*>q;
        q.push(root);
        bool found=false;
        while(!q.empty())
        {
            auto t=q.front();
            q.pop();
            if(!t)found=true;//1.第一次遇见空指针t时标记
            else//2.此时t不是空指针
            {
                if(found)return false;//只要t(此时t不是空指针))之前出现了空指针，说明不是完全二叉树
                q.push(t->left);
                q.push(t->right);
            }
        }
        return true;
    }
};
```

# 复杂度分析
