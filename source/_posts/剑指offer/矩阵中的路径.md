---
title: "矩阵中的路径"
date: 2020-03-04T14:14:06+08:00
categories:
  - 剑指offer
tags:
  - 搜索
---

# 题目

![](/images/%E5%89%91%E6%8C%87offer/%E7%9F%A9%E9%98%B5%E4%B8%AD%E7%9A%84%E8%B7%AF%E5%BE%84.png)

# 综述

[参考链接](https://www.acwing.com/solution/content/728/)

# Code

```c++
class Solution {
public:
    int dx[4]={-1,0,1,0},dy[4]={0,1,0,-1};
    bool exist(vector<vector<char>>& board, string word) {
        for(int i=0;i<board.size();i++)
            for(int j=0;j<board[0].size();j++)
                if(dfs(board,word,0,i,j))
                    return true;

        return false;
    }

    bool dfs(vector<vector<char>>& board, string &word,int u,int x,int y)
    {  
        if(word[u]!=board[x][y])return false;
        if(u==word.size()-1)return true;
        char c=board[x][y];
        board[x][y]='*';
        for(int i=0;i<4;i++)
        {
            int tx=dx[i]+x,ty=dy[i]+y;
            if(tx<0||tx>=board.size()||ty<0||ty>=board[0].size())continue;
            if(dfs(board,word,u+1,tx,ty))return true;
        }
        board[x][y]=c;
        return false;
    }
};
```

# 复杂度分析
