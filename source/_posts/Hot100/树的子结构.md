---
title: "树的子结构"
date: 2020-03-11T00:16:18+08:00
categories:
  - 剑指offer
tags:
  - 树
---

# 题目

![](/images/%E5%89%91%E6%8C%87offer/%E6%A0%91%E7%9A%84%E5%AD%90%E7%BB%93%E6%9E%84.png)

# 综述

# Code

```plain
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */

class Solution {
public:
    bool isSubStructure(TreeNode* A, TreeNode* B) {
        if (!A || !B) return false;
        return dfs(A, B) || isSubStructure(A->left, B) || isSubStructure(A->right, B);
        //注意这里的写法
    }
    
    bool dfs(TreeNode* A, TreeNode* B) {
        if (!B) return true; //该判断只能写在最前面
        if (!A) return false;
        return A->val == B->val && dfs(A->left, B->left) && dfs(A->right, B->right);
    }
};
```

# 复杂度分析
