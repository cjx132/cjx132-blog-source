---
title: "根据身高重建队列"
date: 2020-09-06T22:51:04+08:00
categories:
  - acwing力扣打卡第二期
tags:
  - 贪心
---

# 题目

![](/images/Top100/%E6%A0%B9%E6%8D%AE%E8%BA%AB%E9%AB%98%E9%87%8D%E5%BB%BA%E9%98%9F%E5%88%97.png)

# 综述

-   见注释

# Code

```c++
bool cmp(vector<int>&a ,vector<int>&b){//在一个类中，此函数需要声明为static
    if(a[0]==b[0])return a[1]<b[1];
    return a[0]>b[0];
}
class Solution {
public:
    /**
     * 解题思路：先排序再插入
     * 1.排序规则：按照先H高度降序，K个数升序排序
     * 2.遍历排序后的数组，根据K插入到K的位置上
     *
     * 核心思想：高个子先站好位，矮个子插入到K位置上，前面肯定有K个高个子，矮个子再插到前面也满足K的要求
     */
    vector<vector<int>> reconstructQueue(vector<vector<int>>& people) {
        // [7,0], [7,1], [6,1], [5,0], [5,2], [4,4]
        // 再一个一个插入。
        // [7,0]
        // [7,0], [7,1]
        // [7,0], [6,1], [7,1]
        // [5,0], [7,0], [6,1], [7,1]
        // [5,0], [7,0], [5,2], [6,1], [7,1]
        // [5,0], [7,0], [5,2], [6,1], [4,4], [7,1]

        sort(people.begin(),people.end(),cmp);
        vector<vector<int> > ans;
        for(auto p:people){
            ans.insert(ans.begin()+p[1],p);
        }
        return ans;
    }
};
```

# 复杂度分析

-   时间复杂度：O(nlogn)
    
-   空间复杂度：O(n)
