---
title: "数据流中的中位数"
date: 2020-03-26T12:08:16+08:00
categories:
  - 剑指offer
tags:
  - 堆
---

# 题目

![](/images/%E5%89%91%E6%8C%87offer/%E6%95%B0%E6%8D%AE%E6%B5%81%E4%B8%AD%E7%9A%84%E4%B8%AD%E4%BD%8D%E6%95%B0.png)

# 综述

用大顶堆+小顶堆方法，可以看作大顶堆是普通班，小顶堆是实验班。数量上时刻保持 小顶-大顶<=1（两堆相等或者小顶比大顶多一个）。

新学生先入普通班（大顶堆），此时可能会失去平衡了，于是取大顶堆的第一个（班里最好的学生）加入实验班（小顶堆），判断若数量过多（不是等于或多一个），取第一个（实验班里最差的学生）到普通班（大顶堆）里。 取中位数的时候，若两堆数量相等，则各取堆顶取平均，若小顶比大顶多一，则多的那一个就是中位数。

# Code

```c++
class MedianFinder {
public:
    /** initialize your data structure here. */
    priority_queue<int>high;//大顶堆
    priority_queue<int,vector<int>,greater<int> >low;//小顶堆
    MedianFinder() {

    }
    
    void addNum(int num) {
        high.push(num);
        low.push(high.top());
        high.pop();
        //以上三条语句是有必要的，保证从左边取出最大的，加入到右边
        if(low.size()-high.size()>1){
            high.push(low.top());
            low.pop();
        }
    }
    
    double findMedian() {
        //小顶堆元素总是大于等于大顶堆元素
        if(low.size()>high.size())return (double)low.top();
        else return 0.5*(low.top()+high.top());
    }
};

/**
 * Your MedianFinder object will be instantiated and called as such:
 * MedianFinder* obj = new MedianFinder();
 * obj->addNum(num);
 * double param_2 = obj->findMedian();
 */
```

# 复杂度分析

-   时间复杂度：O(logn) **堆的插入和删除O(logn)** **, 查找中位数需要O(1)**
-   空间复杂度：O(n)
